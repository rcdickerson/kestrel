No result.

Original spec.txt
  pre: left.B == right.B && left.C == right.C && left.N == right.N && left.x == right.x;
  post: left.x == right.x;

Updated spec.txt
  pre: left.B == right.B && left.C == right.C && left.N == right.N && left.x == right.arg3;
  post: left.x == (int)(*((unsigned int*)(&right.x)));

right.x is mutable in the Rust signature, and so it lost its name.
Used right.arg3 in the pre condition instead.

This compiles and reaches SeaHorn, but never returns a result. 
I messed around with bounding the variables in the
pre condition, but bounding all to a smaller domains
didnt caused it to finish.